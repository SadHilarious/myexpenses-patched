name: Build Patched MyExpenses

on:
  push:
    branches: [ main, master ]
  workflow_dispatch: # Allows manual trigger

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Patch Repository
        uses: actions/checkout@v4
        with:
          path: patch-repo

      - name: Checkout Original MyExpenses Repository
        uses: actions/checkout@v4
        with:
          repository: mtotschnig/MyExpenses
          path: src
          # Optionally checkout a specific branch or tag if needed
          # ref: master 

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Run Patch Script
        run: |
          python patch-repo/patch.py src/myExpenses/src/main/java/org/totschnig/myexpenses/util/licence/LicenceHandler.kt

      - name: Enable Google Drive Module and Dynamic Features
        working-directory: src
        run: |
          # Uncomment drive module in settings.gradle
          sed -i "s/\/\/include ':drive'/include ':drive'/g" settings.gradle
          
          # Add drive to dynamicFeatures in myExpenses/build.gradle
          sed -i "s/dynamicFeatures = \[':ocr'/dynamicFeatures = \[':drive', ':ocr'/g" myExpenses/build.gradle

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x src/gradlew

      - name: Build Universal Debug APK
        working-directory: src
        run: ./gradlew myExpenses:packageExternDebugUniversalApk

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: myexpenses-patched-universal-debug
          path: src/myExpenses/build/outputs/apk_from_bundle/externDebug/*.apk
